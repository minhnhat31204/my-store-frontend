"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { getStoredUser, User } from "@/lib/api";
import CustomerNav from "@/app/components/CustomerNav";
export default function AccountPage(){const[user,setUser]=useState<User|null>(null);useEffect(()=>setUser(getStoredUser()),[]);function logout(){localStorage.removeItem("user");setUser(null)}return <main className="min-h-screen bg-slate-50 pb-24 text-slate-900"><CustomerNav/><div className="mx-auto max-w-3xl px-4 py-8 sm:px-6"><h1 className="text-4xl font-black">Tài khoản</h1><div className="mt-8 rounded-2xl bg-white p-6 shadow-sm">{user?<><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-blue-100 text-2xl font-black text-blue-700">{(user.FullName||user.Email).charAt(0).toUpperCase()}</div><div><h2 className="text-xl font-bold">{user.FullName||"Khách hàng"}</h2><p className="text-slate-500">{user.Email}</p></div></div><div className="mt-6 grid gap-3"><Link href="/customer/orders" className="rounded-xl border px-4 py-3 font-semibold hover:bg-slate-50">Lịch sử đơn hàng →</Link><button onClick={logout} className="rounded-xl bg-red-600 px-4 py-3 font-bold text-white">Đăng xuất</button></div></>:<><p className="text-slate-600">Bạn chưa đăng nhập.</p><Link href="/login" className="mt-5 inline-block rounded-xl bg-blue-700 px-5 py-3 font-bold text-white">Đăng nhập</Link></>}</div></div></main>}
